[project]
name = "selenium-reference-framework"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.5",
    "selenium>=4.36.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
pythonpath = [
    "."
]
