# Selenium Reference Framework

A reference Selenium 4 + pytest framework: every major WebDriver API — including BiDi network and log interception, WebAuthn, Shadow DOM — wired into a layered Page Object architecture and verified in CI.
